<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import CtaBlock from './CtaBlock.svelte';

	const { Story } = defineMeta({
		title: 'sections/HeroSection/CtaBlock',
		component: CtaBlock
	});
</script>

<Story name="Default" args={{}} />
