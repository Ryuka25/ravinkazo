<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import CultiverDemainSection from './CultiverDemainSection.svelte';

	const { Story } = defineMeta({
		title: 'sections/CultiverDemainSection',
		component: CultiverDemainSection
	});
</script>

<Story name="Default" args={{}} />
